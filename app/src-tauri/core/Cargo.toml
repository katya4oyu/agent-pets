[package]
name = "agent-pets-core"
version = "0.1.0"
description = "Pure, framework-free core (event schema, normalization, world model) for Agent Pets / Navi"
edition = "2021"

[lib]
name = "agent_pets_core"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
