{
  "name": "navi-playground",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Standalone browser sandbox for tuning the navi pet + UI design (no Tauri). Cloudflare deploy target.",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5173 --strictPort",
    "build": "tsc && vite build",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "@lobehub/icons-static-svg": "^1.90.0",
    "@navi/ui": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^8.1.0"
  }
}
